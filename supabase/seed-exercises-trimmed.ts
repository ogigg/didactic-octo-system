/**
 * Backward-compatible wrapper for the old trimmed-catalog command.
 *
 * The reviewed top-100 catalog in `data/exercises.json` is now the canonical
 * source. Keep this script so existing docs or shell history still seed the
 * current catalog instead of reintroducing the older inline list.
 */

import "./seed-exercises";
